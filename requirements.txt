# LSC (直播切片多人) — Python dependencies
#
# Install with:  pip install -r requirements.txt
# For development/testing:  pip install -r requirements.txt pytest

# 注：PySide6 已移除。历史遗留组件 lsc/gui/pages/recording_controller.py 与
# lsc/gui/common_workers.py 顶部通过 try/except 切换 lsc.gui.qt_compat
# 标准库兼容层（QThread/QTimer/Signal），无 Qt 环境（Store/内置包）亦可运行。

# Python WebSocket backend (Electron version)
websockets>=12.0

# Audio cross-correlation alignment (numpy-based FFT)
numpy>=1.24,<3

# System resource monitoring (CPU, memory, disk)
psutil>=5.9,<7

# 剪映草稿生成（仅生成草稿，不做自动导出）
pyJianYingDraft==0.3.0

# Test dependencies (uncomment if running the test suite)
# pytest>=7.4,<9
