"""LSC GUI 模块（已弃用）。

.. deprecated::
    PySide6 原生 GUI 已弃用，Electron 为唯一前端。
    此包保留供历史参考，不再维护。

    - RoomSession 已迁移至 lsc.core.session
    - RoomOrchestrator 位于 lsc.core.orchestrator
    - MultiRoomManager / RecordingController / workers 均为死代码
"""
