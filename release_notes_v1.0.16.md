## LSC v1.0.16

### 修复（全功能真机验收四缺陷 + 回放×持续分析修复）

- **设置缓存毒化（P0）**：修复设置保存后 mtime 未失效导致的连接全挂崩溃。
- **回看远目标定位**：本地文件回看死线改为停滞看门狗，出画前读取块放大 4× 加速追赶。
- **草稿重叠去重对账**：收尾补扫合成重叠重复候选进 `excluded_clips`，残差字段对齐。
- **切片标签撞号修复**：标签序号采用 per-room 单调计数器，upsert 记忆首发标签，彻底杜绝不同回合撞号。
- **平台抓取统一默认直连**：显式空 ProxyHandler 强制直连，防止死代理导致的解析失败。

### 持续分析与赛事草稿链路优化（v1.0.12 - v1.0.15 累积）
- 终态权威快照跨会话保留，解决已定稿切片被静默丢弃的问题。
- 扫描通路终态补投影，草稿生成原因可辨与结构化分类。
- 导出门禁对齐后端，修复赛事切片导出被多余确认拦截。
- 官方解说分支持续分析的在线微步骤与媒体预算约束优化。

### 安装与发布包说明
- **Windows 安装包**：`LSC.Setup.1.0.16.exe`（或 `LSC 直播切片系统 Setup 1.0.16.exe`），带一键安装与自愈运行时。
- **Microsoft Store (AppX/MSIX)**：`LiveStreamClipper-1.0.16.appx`，自包含全量 Python 与 FFmpeg 运行时，合规 Microsoft Store 政策 10.2.5 与高分屏磁贴规范。
