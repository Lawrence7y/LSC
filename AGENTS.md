# AGENTS.md

> **本文档已迁移至根目录 `CLAUDE.md`，请以此为准。**
>
> CLAUDE.md 是 AI 辅助开发的唯一权威参考，包含完整的架构约束、通信协议、安全防御、错误处理规范。
>
> 本文件保留仅为兼容部分 AI 工具（如 Cursor、OpenCode）的自动发现机制。

请直接阅读 [CLAUDE.md](./CLAUDE.md)。

---

## AI 协作工作规范

> 本节面向所有参与本项目开发的 AI 编码助手（Claude Code / Cursor / OpenCode 等），
> 把优秀工程师的工作习惯固化下来。**每次会话开始前阅读，工作中遵守。**

### 沟通与表达

- **解释复杂内容时善用可视化**：用图表、表格、对比、流程描述代替大段文字；涉及代码位置时给出文件与行号。
- **保持简洁直接，区分事实和猜测**：基于代码事实、运行日志、测试结果说话；推断与假设必须明确标注为"猜测/待验证"，不得与事实混写。
- **基于可靠来源进行研究**：优先查阅源码、运行日志（`%APPDATA%\lsc-electron\logs\`）、官方文档与测试；不凭印象臆断行为。

### 目标与提问

- **不偏离用户目标和约束**：动手前明确任务边界；中途变更范围必须先说明影响。
- **不要随意提问，只有关键决策才确认**：常规事项自行判断推进；仅在影响面大（如重构核心编排、修改协议、改发布策略）或存在多选方案时才向用户确认。

### 工作方式

- **合理使用子 Agent，避免无意义并行**：独立的大块工作（分域审查、批量迁移）可以并行；相互依赖或有共享上下文的工作不要拆散。
- **修改代码保持克制，不做无关重构**：最小改动解决目标问题；顺手"优化"不属于本任务的范围时不做；改动前先读懂上下文。
- **必须测试真实结果，而不是"看起来完成"**：改完必须跑相关测试（pytest / vitest / tsc）或实际复现验证；报告里写明验证方式与结果。

### 保护与汇报

- **保护已有代码和数据**：不破坏既有功能语义；不覆盖用户数据（settings、rooms.json、录制文件、日志）；清理操作先确认。
- **汇报关键结果，不刷无意义进度**：汇报结论、证据、影响面与遗留风险；避免过程性刷屏。
