## 2026-06-28 程序启动

- 成功启动 Python 后端（WebSocket ws://localhost:9876）和 Vite 前端开发服务器（http://localhost:5173/）
- Electron 因 `NODE_OPTIONS=--use-system-ca` 环境变量冲突（Node v18 不兼容）无法启动
- 临时方案：创建了 `vite.dev.config.ts`（不含 `vite-plugin-electron`），用 `npx vite --config vite.dev.config.ts` 替代 `npm run dev`
- 前端通过浏览器即可访问，后端 WebSocket 直连正常
