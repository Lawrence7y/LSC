# 多房间工作台重构与单房间页面合并计划（更新版）

## 摘要

本计划解决多房间工作台页面的多个 UI 问题和架构复用问题。**前一轮已完成部分工作**，本更新版标记已完成项并细化剩余工作。

### 已完成 ✅
1. ✅ 修复预览区点击预览后无画面（`start_preview` 拆分 + `play_preview_stream` 延迟播放）
2. ✅ 修复静音徽章不更新（`room_card.py` 添加 `refresh()` 调用）
3. ✅ 修复"全部静音"按钮状态不同步（`_on_mute_toggled` + `_sync_mute_all_button`）
4. ✅ 直播间每排最多 2 个（`FlowLayout` 添加 `max_per_row=2`，`_update_grid_columns` 改为 `min(2, ...)`）
5. ✅ 底部总状态栏水平滚动条（`QScrollArea` 包裹）
6. ✅ 删除 ControlBar 中的"试听选区"和"回直播"按钮及信号
7. ✅ 删除 `multi_room/page.py` 中的 `return_live`/`preview_range` 信号连接和方法

### 待完成 ❌
1. ❌ 删除 `manager.py` 中的 `range_loop` 底层代码
2. ❌ 回直播浮窗按钮（拖动进度条时右上角出现）
3. ❌ 全屏半透明自动隐藏状态栏验证/调整
4. ❌ 迁移分析功能到 `MultiRoomManager`
5. ❌ 删除 `RecordPage` 及相关文件
6. ❌ 统一预览机制 + 单房间退化
7. ❌ 更新测试套件
8. ❌ 位置节流优化

---

## 当前状态分析（基于代码探查）

### manager.py 中 range_loop 代码现状
以下符号仍完整存在于 `lsc/gui/multi_room/manager.py`：

| 符号 | 行号 | 说明 |
|------|------|------|
| `_preview_loop_timer` | 343（声明）, 1120-1123（创建）, 1136-1138（清理） | QTimer |
| `_loop_room_id` | 344, 564, 977, 1108, 1139, 1141, 1143, 1147, 1149, 1154, 1156 | 当前循环房间 |
| `_loop_start` / `_loop_end` / `_loop_native` | 345-347 | 循环参数 |
| `start_range_loop` | 1101 | 启动方法 |
| `stop_range_loop` | 1125, 调用于 565/978/1107/1152/1156 | 停止方法 |
| `is_range_loop_active` | 1141 | 状态查询 |
| `_on_loop_tick` | 1145 | 定时器回调 |

**关键发现**：`range_loop` 仅在 `manager.py` 内部使用，UI 层已清理完毕，无测试引用。可安全删除。

### FullscreenPreview 现状
- **已有自动隐藏**：2400ms 定时器，鼠标活动唤醒（`_FullscreenActivityFilter`）
- **已有半透明按钮**：`QColor(0, 0, 0, 165 if underMouse else 120)`
- **控制条容器**：`objectName="fullscreenPlayerControls"`，已设 `WA_StyledBackground`
- **MultiRoomPage 使用 builtin 模式**：`get_controls=lambda: None`
- **待验证**：控制条容器背景是否半透明（取决于 theme.py QSS）

### Timeline 拖拽检测
- `_drag_mode` 状态机支持 4 种模式：`'position'`/`'start'`/`'end'`/`'range'`
- 拖动播放头时 emit `position_changed(float)`
- 可用于检测"用户主动 seek 偏离直播边缘"

### RecordPage 独有功能
- **分析**：`_on_analyze_current` → `RecordingController.start_analysis`（`AnalysisWorker` 调用 `lsc.cli.cmd_analyze`）
- **批量导出**：`_on_export_analysis_results` → `RecordingController.start_export_all`（`BatchExportWorker`）
- **磁盘预检**：RecordPage 硬编码 2GB；`RecordingController.preflight_recording` 用 8GB/路
- **重连**：Token 防重入 + 最多 3 次（多房间已有更完善的指数退避）

### main.py 耦合点
- 第 30 行：`from lsc.gui.pages.record import RecordPage`
- 第 203 行：`self._record = RecordPage()`
- 第 208 行：`self._stack.addWidget(self._record)` (index 2)
- 第 36-41 行：`PAGE_MAP` 含 `"record": 2`
- 第 235/246 行：titles 字典含 `"record": "直播录制"`
- 第 255 行：快捷键注册含 `"record"`
- 第 273-317 行：`_refresh_dashboard` 读取 `self._record._ctrl` 状态
- 第 344-356 行：`closeEvent` 清理 RecordPage

### sidebar.py
- 第 29 行：`("record", "直播录制", ...)` 导航项需移除
- 快捷键需从 Ctrl+3(录制)/Ctrl+4(设置) 调整为 Ctrl+3(设置)

### 测试文件影响
引用 RecordPage/RecordingController 的测试文件：
- `tests/test_gui_page_audit.py`
- `tests/gui/test_record_interactions.py`
- `tests/test_e2e_platform_test.py`
- `tests/test_recording_controller_options.py`
- `tests/test_gui_page_reexports.py`
- `test_real_scenario.py`

---

## 提议变更（剩余工作）

### 阶段 3.1：删除 manager.py 中的 range_loop 代码
**文件**: `lsc/gui/multi_room/manager.py`

1. **删除初始化字段**（第 342-347 行）：
   ```python
   # 删除以下 5 行
   self._preview_loop_timer: QTimer | None = None
   self._loop_room_id: str | None = None
   self._loop_start: float = 0.0
   self._loop_end: float = 0.0
   self._loop_native: bool = False
   ```

2. **删除 `remove_room` 中的调用**（第 564-565 行）：
   ```python
   # 删除
   if self._loop_room_id == room_id:
       self.stop_range_loop()
   ```

3. **删除 `set_preview_muted` 中的调用**（第 977-978 行）：
   ```python
   # 删除
   if self._loop_room_id == room_id:
       self.stop_range_loop()
   ```

4. **删除方法定义**（第 1099-1156 行）：
   - `start_range_loop(self, room_id, start, end)`
   - `stop_range_loop(self)`
   - `is_range_loop_active(self)`
   - `_on_loop_tick(self)`

5. **清理 `_sync_range_state`**：在 `page.py` 中已清理 range_loop 逻辑，确认无残留引用。

### 阶段 3.2：回直播浮窗按钮
**目标**：用户拖动进度条偏离直播边缘时，在视频预览区右上角显示"回直播"浮窗按钮，点击跳到最新画面。

#### 3.2.1 在 PreviewSurface 中添加浮窗
**文件**: `lsc/gui/components/preview_surface.py`

- 添加 `_return_live_btn: QPushButton | None` 字段
- 添加 `set_return_live_widget(widget, on_click)` 方法：
  - 将按钮 reparent 到 PreviewSurface
  - 定位在右上角（badge 下方，`margin=6`）
  - 默认 `setVisible(False)`
- 在 `_update_badge_geometry` 中同步定位浮窗按钮
- 添加 `show_return_live(visible: bool)` 方法供外部调用

#### 3.2.2 在 RoomCard 中创建浮窗按钮
**文件**: `lsc/gui/components/room_card.py`

- 在 `_build_ui` 中创建 `_return_live_btn`：
  ```python
  self._return_live_btn = QPushButton("回直播", self._preview_area)
  self._return_live_btn.setObjectName("returnLiveButton")
  self._return_live_btn.setCursor(Qt.PointingHandCursor)
  self._return_live_btn.setVisible(False)
  self._return_live_btn.clicked.connect(
      lambda: self.return_live_requested.emit(self.room.room_id)
  )
  self._preview_area.set_return_live_widget(self._return_live_btn)
  ```
- 添加信号：`return_live_requested = Signal(str)`
- 添加 `set_return_live_visible(visible: bool)` 方法

#### 3.2.3 在 MultiRoomPage 中连接逻辑
**文件**: `lsc/gui/pages/multi_room/page.py`

- 在 `_add_card` 中连接 `card.return_live_requested` → `_on_return_live`
- 添加 `_on_return_live(room_id)` 方法：
  ```python
  def _on_return_live(self, room_id: str) -> None:
      """跳转到最新直播画面。"""
      duration = self._manager.get_preview_duration(room_id)
      if duration > 0:
          self._manager.seek_preview(room_id, duration)
      card = self._cards.get(room_id)
      if card:
          card.set_return_live_visible(False)
  ```
- 在 `_update_card_timeline` 中检测偏离：
  ```python
  def _check_return_live(self, room_id: str, position: float, duration: float) -> None:
      """偏离直播边缘超过 5 秒时显示回直播浮窗。"""
      card = self._cards.get(room_id)
      if card is None or duration <= 0:
          return
      behind = duration - position
      card.set_return_live_visible(behind > 5.0)
  ```
- 在 `_on_global_tick` 和 `_on_card_timeline_seek` 中调用 `_check_return_live`

#### 3.2.4 全屏中的回直播浮窗
**文件**: `lsc/gui/components/fullscreen_preview.py`

- 在 `_build_builtin_controls` 中创建 `_return_live_btn`（定位在 surface 右上角）
- 在 `_sync_builtin` 中根据 `position vs duration` 判断是否显示
- 点击时调用 `on_seek(duration)` 并隐藏

#### 3.2.5 样式
**文件**: `lsc/gui/theme.py`

- 添加 `returnLiveButton` QSS 样式：
  ```css
  #returnLiveButton {
      background: rgba(255, 140, 0, 200);
      color: white;
      border: none;
      border-radius: 4px;
      padding: 4px 10px;
      font-size: 12px;
      font-weight: bold;
  }
  #returnLiveButton:hover {
      background: rgba(255, 140, 0, 230);
  }
  ```

### 阶段 2.3：全屏半透明自动隐藏状态栏验证
**文件**: `lsc/gui/components/fullscreen_preview.py`, `lsc/gui/theme.py`

1. **验证**：确认 `fullscreenPlayerControls` 的 QSS 背景为半透明 `rgba(0, 0, 0, 180)`
2. **如缺失则添加**：在 theme.py 的 `generate_stylesheet` 中添加：
   ```css
   #fullscreenPlayerControls {
       background: rgba(0, 0, 0, 180);
       border-top: 1px solid rgba(255, 255, 255, 30);
   }
   ```
3. **确认自动隐藏**：2400ms 定时器 + 鼠标活动唤醒已正常工作（代码已有，仅需验证）

### 阶段 4.1：迁移分析功能到 MultiRoomManager
**目标**：将 RecordPage 的分析/批量导出能力迁移到 MultiRoomManager，使多房间工作台支持按房间分析。

#### 4.1.1 保留 Worker 类
**文件**: `lsc/gui/pages/recording_controller.py`（保留，不删除）

- `AnalysisWorker`、`BatchExportWorker`、`ExportWorker` 保留在此文件中
- MultiRoomManager 通过 import 使用这些类

#### 4.1.2 在 RoomSession 中添加分析字段
**文件**: `lsc/gui/multi_room/session.py`

```python
# 添加字段
analysis_highlights: list = []          # 分析高光片段列表
analysis_result_path: str = ""          # 分析结果 JSON 路径
analysis_in_progress: bool = False       # 分析是否进行中
export_in_progress: bool = False        # 批量导出是否进行中
```

#### 4.1.3 在 MultiRoomManager 中添加分析方法
**文件**: `lsc/gui/multi_room/manager.py`

```python
def start_analysis(self, room_id: str, profile_name: str, output_dir: str,
                   on_done: Callable | None = None) -> bool:
    """启动指定房间的录制文件分析。"""
    room = self.get_room(room_id)
    if room is None or not room.record_output_path:
        return False
    if room.analysis_in_progress:
        return False
    room.analysis_in_progress = True
    worker = AnalysisWorker(room.record_output_path, profile_name, output_dir)
    # ... 连接信号，启动线程 ...
    return True

def start_export_all(self, room_id: str, output_dir: str,
                     on_done: Callable | None = None) -> bool:
    """批量导出指定房间的分析高光片段。"""
    room = self.get_room(room_id)
    if room is None or not room.analysis_highlights:
        return False
    # ... 使用 BatchExportWorker ...
    return True
```

#### 4.1.4 在 MultiRoomPage 中接入分析 UI
**文件**: `lsc/gui/pages/multi_room/page.py`

- 启用 `_record_analyze_btn`（第 197 行，当前 `setEnabled(False)`）
- 连接 `_record_analyze_btn.clicked` → `_on_analyze_current`
- 添加 `_on_analyze_current` 方法：对当前选中房间启动分析
- 添加 `_on_analysis_done` 回调：存储结果到 RoomSession，在 DetailPanel 中展示
- 启用 `_record_export_analysis_btn`，连接到 `_on_export_analysis_results`
- 在 DetailPanel 中添加分析结果展示区域（复用 `AnalysisResultsGrid` 或简化展示）

#### 4.1.5 磁盘预检统一
**文件**: `lsc/gui/multi_room/manager.py`

- 在 `start_recording` 中调用 `RecordingController.preflight_recording(output_dir, concurrent_streams)`
- 统一使用 8GB/路 标准（替代当前 2GB 硬编码）
- 不足时设置 `room.last_error` 并返回 False

### 阶段 4.2：删除 RecordPage
**目标**：完全移除 RecordPage，所有功能由 MultiRoomPage 承担。

#### 4.2.1 更新 main.py
**文件**: `main.py`

1. 删除导入（第 30 行）：`from lsc.gui.pages.record import RecordPage`
2. 删除实例化（第 203 行）：`self._record = RecordPage()`
3. 删除添加到 stack（第 208 行）：`self._stack.addWidget(self._record)`
4. 从 `PAGE_MAP` 删除 `"record": 2`，调整 settings 索引为 2
5. 从 titles 字典（第 235/246 行）删除 `"record": "直播录制"`
6. 从快捷键列表（第 255 行）删除 `"record"`
7. `_refresh_dashboard`：删除对 `self._record._ctrl` 的引用（第 273-317 行），改为从 MultiRoomManager 获取录制状态
8. `closeEvent`：删除 RecordPage 清理代码（第 344-356 行）

#### 4.2.2 更新 sidebar.py
**文件**: `lsc/gui/components/sidebar.py`

- 从 `_NAV_ITEMS` 删除 `("record", "直播录制", ...)`（第 29 行）
- 调整 settings 快捷键从 Ctrl+4 改为 Ctrl+3
- 保留 3 个导航项：仪表盘(Ctrl+1)、工作台(Ctrl+2)、设置(Ctrl+3)

#### 4.2.3 删除文件
- `lsc/gui/pages/record/page.py`
- `lsc/gui/pages/record/video_preview.py`
- `lsc/gui/pages/record/config_panel.py`
- `lsc/gui/pages/record/icon_widgets.py`
- `lsc/gui/pages/record/__init__.py`
- `lsc/gui/pages/record.py`（re-export 包装）
- `lsc/gui/pages/record_page.py`（如果存在，另一个 re-export）

**保留文件**：
- `lsc/gui/pages/recording_controller.py` — Worker 类供 MultiRoomManager 使用

### 阶段 4.3：统一预览机制 + 单房间退化
**文件**: `lsc/gui/pages/multi_room/page.py`

- 当房间数 = 1 时：
  - 隐藏"批量录制"/"批量停止"按钮（单房间直接用卡片上的录制按钮）
  - 隐藏"全部静音"/"对齐直播"按钮（单房间无意义）
  - 卡片自动占满可用宽度（FlowLayout 单项自动扩展）
- 当房间数 > 1 时：恢复多房间批量操作 UI
- 在 `_refresh` 中根据 `len(self._manager.list_rooms())` 切换 UI

### 阶段 4.4：更新测试套件
**文件**: `tests/`

1. **删除/更新引用 RecordPage 的测试**：
   - `tests/test_gui_page_audit.py`：移除 RecordPage 审计项
   - `tests/gui/test_record_interactions.py`：删除或改为测试 MultiRoomPage 分析功能
   - `tests/test_e2e_platform_test.py`：更新页面引用
   - `tests/test_recording_controller_options.py`：保留（Worker 类仍在）
   - `tests/test_gui_page_reexports.py`：移除 record 模块 re-export 测试
   - `test_real_scenario.py`：更新场景测试

2. **添加新测试**：
   - 回直播浮窗显示/隐藏逻辑
   - 2 列布局验证
   - 分析功能迁移后的接口测试

### 阶段 5.1：位置节流优化
**文件**: `lsc/gui/pages/multi_room/page.py`

- 将 `_POSITION_THRESHOLD` 从 0.5 秒降低到 0.2 秒
- 减少直播位置更新跳动

---

## 实施顺序

1. **阶段 3.1**：删除 manager.py range_loop 代码（无依赖，安全删除）
2. **阶段 3.2**：回直播浮窗按钮（新功能，不影响现有）
3. **阶段 2.3**：全屏半透明验证（仅验证/微调）
4. **阶段 4.1**：迁移分析功能（先迁移，再删源）
5. **阶段 4.2**：删除 RecordPage（确认 4.1 完成后）
6. **阶段 4.3**：单房间退化（UI 优化）
7. **阶段 4.4**：更新测试
8. **阶段 5.1**：位置节流

---

## 假设与决策

1. **range_loop 安全删除**：UI 层已清理，无测试引用，仅 manager.py 内部使用
2. **回直播浮窗位置**：预览区右上角（badge 下方），偏离直播边缘 >5 秒时显示
3. **全屏半透明**：FullscreenPreview 已有自动隐藏机制，仅需验证控制条背景半透明
4. **分析迁移**：保留 `recording_controller.py` 中的 Worker 类，MultiRoomManager import 使用
5. **磁盘预检统一**：使用 `preflight_recording` 的 8GB/路 标准
6. **RecordPage 完全删除**：不保留兼容代码，不保留 re-export
7. **单房间退化**：N=1 时隐藏批量操作 UI，不改变核心逻辑
8. **侧边栏**：从 4 项变为 3 项（仪表盘、工作台、设置）

## 验证步骤

1. **range_loop 清理**：`python -c "from lsc.gui.multi_room.manager import MultiRoomManager; m = MultiRoomManager(); assert not hasattr(m, 'start_range_loop')"`
2. **回直播浮窗**：添加房间 → 预览 → 拖动时间线偏离 5 秒 → 确认右上角浮窗出现 → 点击 → 确认跳到最新
3. **全屏半透明**：进入全屏 → 确认控制条半透明背景 → 2.4 秒后自动隐藏 → 鼠标移动唤醒
4. **分析功能**：录制 → 点击"分析当前录制" → 确认结果展示 → 导出高光
5. **RecordPage 删除**：确认侧边栏无"直播录制"项 → 确认快捷键 Ctrl+1/2/3 对应仪表盘/工作台/设置
6. **单房间退化**：添加 1 个房间 → 确认批量按钮隐藏 → 添加第 2 个 → 确认批量按钮出现
7. **运行测试**：`python -m pytest tests/ --tb=short` 全部通过
