# LSC v1.0.17

> 多直播间**录制 + 切片**一体化工具 —— 同时录制多路直播、跨房间同步预览与选区标记、一键导出片段。

## 🪟 放大预览：控制条一体化 + 自动隐藏

- **时间线与走带按键合成同一块玻璃面板**：原先时间线一行、按键一行各自留白与底色，读起来是两段；现在合并为一块，视觉与交互都是一体。
- **默认向下隐藏，鼠标经过滑出，静止 2.5s 自动收起**：显隐状态机统一走新增的 `useAutoHideControls`；拖动时间线、画质下拉打开期间 `pinned` 钉住，键盘焦点在条内由 CSS `:focus-within` 兜底。隐藏态 `translateY(100%)` + `opacity:0` + `pointer-events:none` 三重收口（不可见、不可点、不挡画面），并补 `prefers-reduced-motion`。

## 📺 新增「窗口播放」= 原生画中画

- 放大态控制条新增**窗口播放**按钮（顺序：静音 → 缩小回网格 → 窗口播放 → 全屏 → 停止预览），与其它覆盖层玻璃按钮同一设计语言，差异化为画中画图标 + 品牌青描边淡青底。
- 走播放器原生 `requestPictureInPicture`：Electron/Chromium 原生小窗**可跨应用常驻**，不需要把 MSE 分片再喂一份。按 `document.pictureInPictureElement` 做 document 级单例判定并监听 document 事件（绑在 video 上会在播放器重建后失效）；回看时跟随回看播放器；卡片卸载时若本房仍在小窗里则收口。
- **一次点击 = 缩小 + 窗口播放**：同一路画面不在卡片与小窗里各播一份；退出小窗只退小窗，不重新放大。

## ⏪ 回放交互口径：能点的范围 = 真能回放的范围

- 修复「点在画出来的窗口内、真实缓冲外」会被判成缓冲外并切到本地文件回看、预览区白等数秒的问题：放大预览条的可点/可拖范围改为**真实可回放范围**——`max(真实缓冲起点, liveEdge − 设置时长)`，回放设置**只作上限**，绝不把未缓冲的历史画成可点区域。
- 新增 `isWithinSeekRange`（**2s 边界容差**）：分片边界/浮点误差不再把一次普通点击推给重量级的回看通道。
- 移除放大预览区误导性的两个时长文案（「回看设置 X · 实际可回放 Y」）；主时间线紫标（真实起点 / 真实可回放量）不变。

## 🔧 后端与分析链

- **赛事分裂碎片合并/对账**：头尾碎片族按几何与结论判据合并，补回被切掉的回合前半段；分裂族账本对账避免重复。
- **直播沿优先**：持续分析落后文件尾过多时优先扫描直播沿窗口，最新回合不必等整段历史爬完。
- **MSE 暂停自愈**：后端推送暂停超过阈值自动恢复（含暂停时长与被丢弃分段数的告警），杜绝「永久只发 init」。
- **日志截断**：大字段/高频状态在落盘前按长度截断，日志不再膨胀。
- **安装包隐私加固**：打包不再携带本地设置、录制历史、日志与 `__pycache__`。

## 📦 下载与安装

| 形态 | 文件 | 说明 |
| :--- | :--- | :--- |
| Windows 安装包 | `LSC.Setup.1.0.17.exe` | 一键安装、可选安装目录；运行依赖首次启动按需下载 |
| Microsoft Store | `LiveStreamClipper-1.0.17.appx` | **自包含**全量 Python 与 FFmpeg 运行时，零联网安装（商店版本由应用商店自动下发） |

## ✅ 质量门禁

- 前端 **vitest 26 文件 / 255 用例全绿**，`tsc --noEmit` 无错误。
- 后端 **pytest 533 用例全绿**（本次改动相关 12 个测试文件）。
- 新增守卫：可点范围=可回放范围 / 原生画中画 / 控制条一体化自动隐藏（3 条后端守卫 + 3 个前端夹具）。

**完整变更**：见 [CHANGELOG.md](https://github.com/Lawrence7y/LSC/blob/main/CHANGELOG.md)
